#!/usr/bin/env bash

# Installs ffmpeg in a Mac. But you can also install ffmpeg in 
# Windows (https://ffmpeg.org/download.html#build-windows) and
# Linux (https://ffmpeg.org/download.html#build-linux)

# Prerequisites for Mac
# xcode and homebrew. Uncomment the lines below if you need those

# xcode
# xcode-select --install

# Homebrew
# /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

# ffmpeg
brew install ffmpeg
